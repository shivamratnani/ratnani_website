/**
 * Horizontal rhythm for the full-bleed shell. Every top-level surface — nav,
 * hero, sections, footer — uses SHELL and nothing else, so the gutters cannot
 * drift apart as pages are added.
 */
export const SHELL = "w-full px-6 sm:px-10 lg:px-16 xl:px-24";

/** Readable line length for running prose inside the full-bleed shell. */
export const MEASURE = "max-w-[68ch]";
